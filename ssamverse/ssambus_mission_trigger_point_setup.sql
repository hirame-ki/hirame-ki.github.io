-- =====================================================================
-- 쌤버스 - 3D 맵 미션 발동 위치(좌표) 컬럼 추가
-- Supabase 대시보드 > SQL Editor 에서 이 파일 내용을 그대로 실행하세요.
-- =====================================================================

-- 3D 맵 전용: 월드 좌표 기반 미션 발동 지점
-- 형식: {"x":5.5, "z":-10.8, "r":2.2}  (x/z 월드좌표 + 판정 반경, 미터 단위)
-- 이 값이 있으면 trigger_tiles/zone_id 대신 (x,z)에서 반경 r 이내로 접근했을 때 미션이 발동됩니다.
-- (2D 맵의 trigger_tiles와 동일한 방식으로 nullable 컬럼만 추가 — 기존 데이터/로직에 영향 없음)
alter table missions
  add column if not exists trigger_point jsonb;
